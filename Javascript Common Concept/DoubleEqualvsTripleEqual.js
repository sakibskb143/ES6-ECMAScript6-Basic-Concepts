// bouble equals check only value , the not check the datatype.
// triple equals check value and datatype as well.
// equal comparism doesn't work for non-primitive datatype(object,array)


const first ='0';
const second = false ;
if(first == second){
    console.log('values are equal');
}
else{
    console.log('values are not equal.')
}